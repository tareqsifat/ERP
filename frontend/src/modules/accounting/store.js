import { defineStore } from 'pinia'

// Pinia store for the Accounting module (sdd.md §2). Filled in as this
// module is implemented.
export const useAccountingStore = defineStore('accounting', {
  state: () => ({}),
})
