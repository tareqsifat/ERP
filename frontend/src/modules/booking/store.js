import { defineStore } from 'pinia'

// Pinia store for the Booking module (sdd.md §2). Filled in as this
// module is implemented.
export const useBookingStore = defineStore('booking', {
  state: () => ({}),
})
