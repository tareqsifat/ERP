import { defineStore } from 'pinia'

// Pinia store for the Budgeting module (sdd.md §2). Filled in as this
// module is implemented.
export const useBudgetingStore = defineStore('budgeting', {
  state: () => ({}),
})
