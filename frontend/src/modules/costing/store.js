import { defineStore } from 'pinia'

// Pinia store for the Costing module (sdd.md §2). Filled in as this
// module is implemented.
export const useCostingStore = defineStore('costing', {
  state: () => ({}),
})
