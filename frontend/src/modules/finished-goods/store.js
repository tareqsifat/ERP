import { defineStore } from 'pinia'

// Pinia store for the FinishedGoods module (sdd.md §2). Filled in as this
// module is implemented.
export const useFinishedGoodsStore = defineStore('finished_goods', {
  state: () => ({}),
})
