// Hrm module routes (sdd.md §2). Filled in as this module is
// implemented per todo.md's phase order — empty until then so
// src/router/index.js can import it unconditionally from Phase 1.
export default []
