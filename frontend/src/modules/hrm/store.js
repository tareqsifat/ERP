import { defineStore } from 'pinia'

// Pinia store for the Hrm module (sdd.md §2). Filled in as this
// module is implemented.
export const useHrmStore = defineStore('hrm', {
  state: () => ({}),
})
