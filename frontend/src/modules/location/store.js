import { defineStore } from 'pinia'

// Pinia store for the Location module (sdd.md §2). Filled in as this
// module is implemented.
export const useLocationStore = defineStore('location', {
  state: () => ({}),
})
