import { defineStore } from 'pinia'

// Pinia store for the Order module (sdd.md §2). Filled in as this
// module is implemented.
export const useOrderStore = defineStore('order', {
  state: () => ({}),
})
