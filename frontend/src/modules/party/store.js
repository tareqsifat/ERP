import { defineStore } from 'pinia'

// Pinia store for the Party module (sdd.md §2). Filled in as this
// module is implemented.
export const usePartyStore = defineStore('party', {
  state: () => ({}),
})
