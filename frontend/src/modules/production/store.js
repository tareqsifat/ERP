import { defineStore } from 'pinia'

// Pinia store for the Production module (sdd.md §2). Filled in as this
// module is implemented.
export const useProductionStore = defineStore('production', {
  state: () => ({}),
})
