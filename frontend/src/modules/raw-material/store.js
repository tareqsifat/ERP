import { defineStore } from 'pinia'

// Pinia store for the RawMaterial module (sdd.md §2). Filled in as this
// module is implemented.
export const useRawMaterialStore = defineStore('raw_material', {
  state: () => ({}),
})
