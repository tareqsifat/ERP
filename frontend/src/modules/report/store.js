import { defineStore } from 'pinia'

// Pinia store for the Report module (sdd.md §2). Filled in as this
// module is implemented.
export const useReportStore = defineStore('report', {
  state: () => ({}),
})
