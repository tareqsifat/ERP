import { defineStore } from 'pinia'

// Pinia store for the Sampling module (sdd.md §2). Filled in as this
// module is implemented.
export const useSamplingStore = defineStore('sampling', {
  state: () => ({}),
})
