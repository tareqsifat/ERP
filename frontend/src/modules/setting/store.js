import { defineStore } from 'pinia'

// Pinia store for the Setting module (sdd.md §2). Filled in as this
// module is implemented.
export const useSettingStore = defineStore('setting', {
  state: () => ({}),
})
