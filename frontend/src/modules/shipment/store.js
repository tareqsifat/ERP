import { defineStore } from 'pinia'

// Pinia store for the Shipment module (sdd.md §2). Filled in as this
// module is implemented.
export const useShipmentStore = defineStore('shipment', {
  state: () => ({}),
})
