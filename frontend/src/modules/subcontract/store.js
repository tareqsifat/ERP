import { defineStore } from 'pinia'

// Pinia store for the Subcontract module (sdd.md §2). Filled in as this
// module is implemented.
export const useSubcontractStore = defineStore('subcontract', {
  state: () => ({}),
})
